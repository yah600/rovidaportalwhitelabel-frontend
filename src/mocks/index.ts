import { MOCKS_ENABLED } from './config';

// This file will export mock data and adapters when MOCKS_ENABLED is true.
// In a real application, you would have a more sophisticated setup for this.

if (MOCKS_ENABLED) {
  console.log('Mocks are enabled');
}